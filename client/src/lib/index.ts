export type Device = {
    name?:string,
    messages?: string ,
    assignedGroup?: string ,
    assignedUser?: string,
    id?: string
}

export type PublicUser = {
    username: string,
    id: string
}
